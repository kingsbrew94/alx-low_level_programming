#include "main.h"

/**
  * _isupper - checks if a letter is in upper case
  * @n: The number to be checked
  *
  * Return: 1 for capital letters or 0 else for any character
  */
int _isupper(int n)
{
if (n >= 65 && n <= 90)
{
return (1);
}
return (0);
}

