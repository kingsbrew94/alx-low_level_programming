#include "main.h"

/**
 * _abs - checks and returns absolute numbers
 * @n: The number to be converted to the absolute value
 *
 * Return: n if number is greater else -n.
 */
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
